spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    #url: jdbc:postgresql://patient-care-diary-db.internal:5432/patient_care_diary?user=postgres&password=FuyPYI34P0GjXGZ
    url: jdbc:postgresql://localhost:5432/pcd-db?characterEncoding=utf8
    username: postgres
    password: 1234
  profiles:
    include: oauth, jwt
  cloud:
    gcp:
      storage:
        credentials:
          location: classpath:patient-care-diary-5fe08c6658e1.json
        project-id: patient-care-diary
        bucket: patient-care-diary-bucket
  jpa:
    database: postgresql
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
    hibernate:
      ddl-auto: create
springdoc:
  packages-to-scan=: com.springboot.domain